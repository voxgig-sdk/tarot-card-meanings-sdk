[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voxgig-sdk-tarot-card-meanings"
version = "0.0.1"
description = "Unofficial generated Python SDK for the Tarot Card Meanings public API. Not affiliated with or endorsed by the upstream API provider."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
keywords = ["voxgig", "sdk", "generated-sdk", "openapi", "api-client", "tarot-card-meanings"]
dependencies = [
    "requests>=2.33",
]

[project.urls]
Homepage = "https://github.com/voxgig-sdk/tarot-card-meanings-sdk"
Repository = "https://github.com/voxgig-sdk/tarot-card-meanings-sdk"
Issues = "https://github.com/voxgig-sdk/tarot-card-meanings-sdk/issues"

# Ship the top-level single-file modules (setuptools find only discovers
# package dirs, never these) so the documented import actually installs.
# tarotcardmeanings_types holds the generated @dataclass models.
[tool.setuptools]
py-modules = ["tarotcardmeanings_sdk", "tarotcardmeanings_types", "config", "features"]

# Explicit package list — setuptools auto-discovery refuses to pick when
# multiple top-level dirs (core/entity/feature/utility) are present.
[tool.setuptools.packages.find]
include = ["core*", "entity*", "feature*", "utility*"]

# Ship the PEP 561 py.typed marker(s) so the inline type hints reach consumers.
[tool.setuptools.package-data]
"*" = ["py.typed"]
